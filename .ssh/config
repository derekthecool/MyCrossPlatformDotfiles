# ssh chooses the first matching host
HOST 192.168.1.*
     USER root
     RequestTTY yes
     RemoteCommand tmux new -A

HOST ogd*
     USER dlomax
     RequestTTY yes
     RemoteCommand tmux new -A

HOST *
     USER dlomax
